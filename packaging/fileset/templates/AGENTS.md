# AGENTS.md — HATHOR OpenSource member (fileset bootstrap)

> Generated by the HATHOR OpenSource fileset. Customize before committing.

| Field | Value |
|-------|-------|
| Group | `hath0r-opensource` |
| Product | `{{PRODUCT_ID}}` |
| Control tower | HATH0R-CLI (`hath0r`) |
| KB mode | stub → group hub (or standalone local stub) |
| Hidden root | `.hath0r/` only |

## Orientation

1. Install the operator CLI: `pipx install hath0r-cli` (or binary from GitHub Releases).
2. Set `HATH0R_GROUP_ROOT` if this tree is not under a discoverable group root.
3. Run `hath0r doctor` and fix named checks.
4. Do not create `.ai/`, `.aegis/`, or `.infraOS/` — use `.hath0r/` only.
5. Never hardcode secrets; use the org credentials layout outside this tree.

## Modes

- **Member mode**: place this project beside other OpenSource members under a group root that contains `AGENTS.md` with `hath0r-opensource` and `.hath0r/knowledgebase`.
- **Standalone mode**: keep the local `.hath0r/knowledgebase` stub; point `HATH0R_KB_PATH` when a hub exists.
