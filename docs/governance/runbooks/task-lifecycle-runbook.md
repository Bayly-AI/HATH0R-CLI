# Runbook — Task lifecycle

## Stuck on canonical branch
- Use `hath0r task start` to create work branch.

## Finish failed quality gates
- `hath0r quality check <pr>`; fix hard gates (Sonar token infra if missing).
