# Playbook — OpenFeature setup

1. Add `cfg/feature-flags/openfeature.json` + catalog.
2. Choose provider (`in-memory` local; vendor in higher envs).
3. Replace ad-hoc env booleans for product behavior with flag keys.
4. Default safe; document owners.
5. Complete `openfeature-adherence.md`.
