# Playbook — Start/end of task

1. `hath0r task start --issue N --slug s` (or `--title` to create issue).
2. Implement on work branch only.
3. `hath0r preflight run` before PR.
4. `hath0r task finish --semver patch`.
