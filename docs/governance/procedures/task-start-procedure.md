# Procedure — Task start

1. Confirm issue exists or create via CLI.
2. `hath0r task start --issue <n> --slug <slug> --prefix feature`.
3. Verify branch `feature/<n>-<slug>`.
4. Proceed with implementation.
